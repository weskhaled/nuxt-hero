// Feature normalization lives in the runtime (shared with the Vue plugin);
// re-exported here for the module side + existing import paths.
export { resolveFeatures, VALID_EFFECTS } from '../runtime/config'
